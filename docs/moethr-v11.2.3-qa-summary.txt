MOETHR V11.2.3 RELIABILITY PATCH QA

Issue reproduced by code audit:
- AUDIT, WHO-5, LEC-5 and WSAS are declared as import placeholders in the base TESTS table, then upgraded to included questionnaires later by the V11 runtime.
- V10.1 empty-draft cleanup runs before that upgrade. It previously used answeredCount(t)>0, which can be zero while the placeholder has no active item count, even when raw saved answers exist.
- This could delete in-progress answers on reload/app termination. AUDIT matches the user-reported failure.
- ASSIST uses a separate structured state path and already received its own persistence guard in V11.2.2.

V11.2.3 fixes:
1. meaningfulProgress101 now treats any non-empty raw state.answers[id] object as real progress before consulting runtime item counts.
2. This protects AUDIT, WHO-5, LEC-5, WSAS, and any future runtime-upgraded/import-transition assessment using normal answer storage.
3. ASSIST remains protected by its dedicated extras-based progress detection and verified persistence.
4. My Plan / Retake & Update tab switching no longer re-renders Home; final patch disables scroll anchoring on the plan card and compensates for Safari/iOS scroll-anchor movement so the tab strip stays in the same viewport position.

Static QA:
- 27 inline JavaScript blocks syntax-checked with Node: PASS.
- V11.2.3 / build 2026.08.22.2 version markers: PASS.
- Runtime-upgraded module persistence guard present: PASS.
- Final plan-tab stability override present: PASS.

Manual device tests still required:
- Start AUDIT, answer 2-3 items, kill PWA, reopen: answers must remain.
- Repeat with WHO-5, LEC-5, WSAS.
- Switch My Plan <-> Retake / Update repeatedly at several Home scroll positions; no visible jump/flicker.
